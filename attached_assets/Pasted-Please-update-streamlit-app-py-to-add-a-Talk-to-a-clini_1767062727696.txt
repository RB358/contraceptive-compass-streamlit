Please update streamlit_app.py to add a “Talk to a clinician” / “Book a telehealth visit” CTA system throughout the user journey, using US English and US telehealth terminology.

GOALS
1) A persistent, non-intrusive floating CTA visible on all screens (landing, quiz, results) on mobile + desktop.
2) Contextual CTAs inside the expanded recommendation details panel (inline details card). Slightly stronger emphasis for “Less likely to be suitable.”
3) Keep an end-of-results CTA, but make it clearer and more professional than “Book Doctor Now.”
4) All CTAs open an external telehealth booking URL in a new tab (for now). Use a single constant BOOK_URL.
5) Use US English spelling and US clinical wording: “clinician,” “telehealth visit,” “appointment,” “provider.”

A) ADD A TELEHEALTH BOOKING URL CONSTANT
Near the top of streamlit_app.py, define:
BOOK_URL = "https://YOUR-US-TELEHEALTH-LINK-HERE"

(If there is already a booking URL somewhere, replace it with this constant.)

B) ADD CSS FOR A FLOATING CTA (PERSISTENT, MOBILE FRIENDLY)
In the main CSS <style> block, add these classes (keep existing palette variables):

.floating-cta{
  position: fixed;
  right: 18px;
  bottom: 18px;
  z-index: 9999;
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 12px 14px;
  border-radius: 999px;
  background: rgba(255, 255, 255, 0.92);
  border: 1px solid rgba(209, 73, 91, 0.35);   /* coral border */
  box-shadow: 0 10px 28px rgba(15, 23, 42, 0.12);
  text-decoration: none;
  color: var(--ink);
  font-weight: 700;
}

.floating-cta:hover{
  border-color: rgba(209, 73, 91, 0.55);
  box-shadow: 0 12px 32px rgba(15, 23, 42, 0.16);
}

.floating-cta .dot{
  width: 10px;
  height: 10px;
  border-radius: 999px;
  background: var(--coral);   /* #D1495B */
  flex: 0 0 auto;
}

.floating-cta .sub{
  font-weight: 600;
  color: rgba(15, 23, 42, 0.70);
  font-size: 0.85rem;
  margin-left: 6px;
}

/* Mobile: center and widen */
@media (max-width: 640px){
  .floating-cta{
    right: 12px;
    left: 12px;
    bottom: 12px;
    justify-content: center;
  }
}

C) RENDER THE FLOATING CTA ON EVERY RUN (TOP-LEVEL)
After the main CSS is injected (and after st.set_page_config), render this floating HTML link so it appears on every screen:

st.markdown(
  f'''
  <a class="floating-cta" href="{BOOK_URL}" target="_blank" rel="noopener noreferrer">
    <span class="dot"></span>
    <span>Talk to a clinician<span class="sub">Book a telehealth visit</span></span>
  </a>
  ''',
  unsafe_allow_html=True
)

NOTE: This must be placed outside any conditional blocks so it always renders.

D) ADD CONTEXTUAL CTA INSIDE EXPANDED RECOMMENDATION DETAILS
Inside the inline “details-card” block (where you show the method image + pros/cons), add a contextual CTA section at the bottom.

1) Add a short helper sentence:
- For tier "best" or "consider":
  "If you’d like, you can book a telehealth visit to talk through your options."
- For tier "unlikely":
  "Based on your answers, this option is less likely to fit. A clinician can help you review safer alternatives."

2) Add a prominent, full-width button-style link (teal normally; coral for “unlikely”) using HTML so it opens in a new tab.

Add this CSS to your main CSS block:

.details-cta{
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  margin-top: 10px;
  padding: 12px 14px;
  border-radius: 999px;
  background: var(--teal);
  border: 1px solid var(--teal);
  color: white !important;
  text-decoration: none;
  font-weight: 750;
  width: 100%;
}

.details-cta:hover{
  background: var(--teal-600);
  border-color: var(--teal-600);
}

.details-cta.unlikely{
  background: var(--coral);
  border-color: var(--coral);
}

.details-cta.unlikely:hover{
  background: var(--coral-hover); /* #E06372 */
  border-color: var(--coral-hover);
}

Then, in the details rendering code, determine tier_key (best/consider/unlikely) and render:

cta_text = "Book a telehealth visit"
cta_class = "details-cta unlikely" if tier_key == "unlikely" else "details-cta"
helper = (
  "Based on your answers, this option is less likely to fit. A clinician can help you review safer alternatives."
  if tier_key == "unlikely"
  else "If you’d like, you can book a telehealth visit to talk through your options."
)

st.markdown(f"<div class='rec-meta' style='margin-top:10px;'>{helper}</div>", unsafe_allow_html=True)
st.markdown(
  f"<a class='{cta_class}' href='{BOOK_URL}' target='_blank' rel='noopener noreferrer'>"
  f"{cta_text} →</a>",
  unsafe_allow_html=True
)

E) IMPROVE END-OF-RESULTS CTA (CLEAR, US ENGLISH)
Where you currently show the understated button at the end, replace it with:

1) Header:
"Want personalized medical advice?"

2) Subtext:
"Book a telehealth visit to discuss your options and medical eligibility."

3) Full-width CTA link:
st.markdown(
  f"<a class='details-cta' href='{BOOK_URL}' target='_blank' rel='noopener noreferrer'>Book a telehealth visit →</a>",
  unsafe_allow_html=True
)

F) OPTIONAL: LIGHT-TOUCH SECONDARY LINK NEAR THE START BUTTON
Near the Start button on the landing page, add a small link:

"Prefer to talk to someone? Book a telehealth visit."

Add CSS:
.inline-link{
  color: var(--coral) !important;
  text-decoration: none;
  font-weight: 700;
}
.inline-link:hover{ text-decoration: underline; }

Then render under Start:
st.markdown(
  f"<a class='inline-link' href='{BOOK_URL}' target='_blank' rel='noopener noreferrer'>Prefer to talk to someone? Book a telehealth visit.</a>",
  unsafe_allow_html=True
)

CONSTRAINTS
- Do not break existing quiz state or recommendation rendering.
- Use BOOK_URL everywhere (single source of truth).
- All booking links open in a new tab.
- Keep the floating CTA calm (white background + coral border), not a large solid button.

DELIVERABLE
- Provide the updated CSS block and updated code insertions (BOOK_URL, floating CTA render, details CTA section, end-of-results CTA replacement).
