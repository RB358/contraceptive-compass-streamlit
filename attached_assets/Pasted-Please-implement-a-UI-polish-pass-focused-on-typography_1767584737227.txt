Please implement a UI polish pass focused on typography, spacing, and button consistency, without changing any quiz logic or navigation behavior.

SCOPE / CONSTRAINTS
- Do NOT change any Python quiz logic, session_state, question flow, or st.columns layout.
- Only adjust CSS + minor markup wrappers if needed for styling (safe, non-structural).
- Keep the overall centered layout.
- Ensure mobile and desktop both look intentional.
- Unify ALL buttons: Start Quiz CTA should share the same design language as quiz Back/Next buttons (same radius, border, hover/focus, typography).

1) UPDATE DESIGN TOKENS + TYPOGRAPHY SCALE
In streamlit_app.py inside the existing <style> block, replace/extend :root with a proper type/spacing system. Keep existing brand colors but add size tokens:

:root{
  --teal:#0F766E;
  --teal-600:#0B5F59;
  --coral:#D1495B;
  --coral-hover:#E06372;
  --ink:#211816;            /* use the dark brown/charcoal for text */
  --surface:#FFFFFF;
  --warm-bg:#FFFBFA;
  --border:#E5E7EB;

  --text-xs:12px;
  --text-sm:14px;
  --text-md:16px;
  --text-lg:18px;
  --text-xl:22px;
  --text-2xl:28px;

  --lh-tight:1.2;
  --lh-normal:1.5;

  --space-1:4px;
  --space-2:8px;
  --space-3:12px;
  --space-4:16px;
  --space-5:20px;
  --space-6:24px;
  --space-8:32px;

  --radius-sm:12px;
  --radius-pill:999px;

  --shadow-1:0 1px 2px rgba(0,0,0,0.06);
  --shadow-2:0 10px 25px rgba(0,0,0,0.08);

  --focus-ring:0 0 0 4px rgba(15,118,110,0.18);
}

Set base font and text colour consistently:
html, body, [class*="stApp"]{
  color: var(--ink);
  background: var(--warm-bg);
  font-family: ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, Helvetica, Arial;
}

2) IMPROVE SPACING + HIERARCHY ON QUIZ SCREEN
- Increase breathing room between progress, question title, helper text, and options.
- Keep text centered, but make spacing rhythm consistent.

Add:
.progress-text{
  font-size: var(--text-sm);
  font-weight: 650;
  margin: var(--space-3) 0 var(--space-4) 0;
  color: var(--coral);
}

.quiz-card{
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 20px;
  padding: var(--space-6);
  margin: var(--space-5) auto;
  box-shadow: var(--shadow-1);
  max-width: 720px;
}

.quiz-card h3{
  font-size: var(--text-xl);
  line-height: var(--lh-tight);
  margin: 0 0 var(--space-2) 0;
  color: var(--ink);
  font-weight: 750;
  text-align: center;
}

.quiz-card .stCaption, .quiz-card [data-testid="stCaptionContainer"]{
  font-size: var(--text-md);
  opacity: 0.85;
  margin-bottom: var(--space-5);
}

.main .block-container{
  max-width: 860px !important;
  padding-top: var(--space-6) !important;
  padding-left: var(--space-6) !important;
  padding-right: var(--space-6) !important;
}

@media (max-width: 640px){
  .main .block-container{
    padding-left: var(--space-4) !important;
    padding-right: var(--space-4) !important;
  }
  .quiz-card{ padding: var(--space-5); }
  .quiz-card h3{ font-size: 20px; }
}

3) OPTION TILE POLISH (INTERACTIVITY + SELECTED STATE)
In the screenshot, answer choices are large tiles. Improve their “button-like” interactivity:
- hover: slightly stronger border + subtle background
- active/click: small press effect
- focus: visible ring for keyboard access
- selected: stronger border and slightly deeper tint

Add these styles targeting Streamlit’s radio/button-like option tiles (keep selectors broad but safe):
/* Generic tile look for option rows */
div[role="radiogroup"] > label,
[data-testid="stRadio"] label{
  border-radius: var(--radius-sm) !important;
  padding: 18px 16px !important;
  border: 1px solid rgba(15,118,110,0.35) !important;
  background: rgba(15,118,110,0.06) !important;
  margin: 12px 0 !important;
  box-shadow: none !important;
  transition: transform 120ms ease, background-color 120ms ease, border-color 120ms ease;
  cursor: pointer;
}

/* Hover */
div[role="radiogroup"] > label:hover,
[data-testid="stRadio"] label:hover{
  background: rgba(15,118,110,0.09) !important;
  border-color: rgba(15,118,110,0.55) !important;
}

/* Selected */
div[role="radiogroup"] > label:has(input:checked),
[data-testid="stRadio"] label:has(input:checked){
  background: rgba(15,118,110,0.12) !important;
  border: 2px solid rgba(15,118,110,0.75) !important;
}

/* Active press */
div[role="radiogroup"] > label:active,
[data-testid="stRadio"] label:active{
  transform: translateY(1px);
}

/* Focus ring for accessibility */
div[role="radiogroup"] > label:has(input:focus-visible),
[data-testid="stRadio"] label:has(input:focus-visible){
  box-shadow: var(--focus-ring) !important;
  outline: none !important;
}

4) BUTTON SYSTEM (UNIFY START QUIZ + QUIZ NAV)
We need one coherent button system:
- Primary button: teal fill, white text (“Start quiz”, “Next”, “See results”)
- Secondary button: white/light surface, teal border + teal text (“Back”, utility buttons)
- Both: same radius/padding/font, consistent hover/focus/active

IMPORTANT: do this without breaking layout. Use wrappers around specific buttons where needed.

Add button base:
.stButton > button{
  border-radius: var(--radius-sm) !important;
  padding: 14px 18px !important;
  font-size: var(--text-md) !important;
  font-weight: 700 !important;
  transition: transform 120ms ease, background-color 120ms ease, border-color 120ms ease, box-shadow 120ms ease;
  box-shadow: var(--shadow-1) !important;
}

.stButton > button:active{ transform: translateY(1px); }
.stButton > button:focus-visible{ box-shadow: var(--focus-ring) !important; outline: none !important; }

Create two wrapper classes so we can assign primary/secondary without relying on fragile selectors:
.cc-primary .stButton > button{
  background: var(--teal) !important;
  border: 1px solid var(--teal) !important;
  color: #fff !important;
}
.cc-primary .stButton > button:hover{
  background: var(--teal-600) !important;
  border-color: var(--teal-600) !important;
}

.cc-secondary .stButton > button{
  background: rgba(255,255,255,0.9) !important;
  border: 1px solid rgba(15,118,110,0.40) !important;
  color: var(--teal) !important;
}
.cc-secondary .stButton > button:hover{
  background: rgba(15,118,110,0.06) !important;
  border-color: rgba(15,118,110,0.60) !important;
}

Now apply wrappers in Python ONLY around the relevant buttons:
- Wrap Start CTA button container in <div class="cc-primary"> ... </div>
- Wrap Next/See Results in cc-primary
- Wrap Back buttons in cc-secondary

Example pattern:
st.markdown('<div class="cc-primary">', unsafe_allow_html=True)
st.button("Start quiz", ...)
st.markdown('</div>', unsafe_allow_html=True)

Do the same for Next and See Results. For Back:
st.markdown('<div class="cc-secondary">', unsafe_allow_html=True)
st.button("← Back", ...)
st.markdown('</div>', unsafe_allow_html=True)

This ensures Start Quiz matches Quiz nav, and Back is clearly secondary.

5) “FIND YOUR CONTRACEPTIVE” COLOUR EMPHASIS (OPTIONAL, TASTE-LIMITED)
If the landing headline contains the phrase “Find your contraceptive”, set ONLY that phrase to teal/coral for subtle hierarchy. Keep it restrained.

Implement by changing the landing hero h1 markup to wrap the phrase in a span:
<h1><span class="hero-accent">Find your contraceptive</span> in seven questions</h1>

Add:
.hero-accent{ color: rgba(255,255,255,0.95); text-decoration: none; }
If you want a colour pop instead (still readable on image), use coral for just the phrase:
.hero-accent{ color: #FFD4D9; }  (very light coral tint for contrast)
Do NOT use saturated coral on the hero image; keep it light for legibility.

6) FINAL CHECKS / ACCEPTANCE CRITERIA
- No quiz logic changes.
- Start button and Next button share identical size, radius, hover, focus ring.
- Back is visibly secondary.
- Options have clear hover + selected + focus-visible states.
- Spacing feels calmer (more breathing room), especially above/below options.
- Mobile: no overlaps; buttons remain in their columns.

Please implement, then post back screenshots of: landing, question screen, and results screen (desktop + mobile width).
